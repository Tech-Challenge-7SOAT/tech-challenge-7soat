apiVersion: v1
kind: Service
metadata:
  name: order-api-service
  labels:
    app: order-api
spec:
  type: LoadBalancer
  selector:
    app: order-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80